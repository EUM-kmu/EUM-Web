import styled from 'styled-components';

export const CardContainer = styled.div`
    display: flex;
    width: 160px;
    padding: 16px 22px;
    flex-direction: column;
    align-items: flex-start;
    gap: 10px; // space-between으로 대체
`